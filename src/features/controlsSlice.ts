import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

// For filter and search controls
const controlsSlice = createSlice({
  name: 'controlsSlice',
  initialState,
  reducers: {},
});

export const {} = controlsSlice.actions;

export default controlsSlice.reducer;
